# pt-techne-development-setup

This repository provides a standardized local development environment for Infrastructure as Code (IaC) contributors. It contains a Docker image (based on the VS Code devcontainer base) with all platform tools pre-installed: OpenTofu, Google Cloud SDK, kubectl, helm, istioctl, gh, pre-commit, and a fully configured zsh shell (Oh My Zsh + Powerlevel10k).
